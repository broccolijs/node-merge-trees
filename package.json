{
  "name": "merge-trees",
  "description": "Incrementally merge directories.",
  "version": "1.0.1",
  "author": "Jo Liss <joliss42@gmail.com>",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/broccolijs/node-merge-trees"
  },
  "keywords": [
    "merge",
    "copy"
  ],
  "files": [
    "index.js",
    "dist/merge-trees.cjs.js"
  ],
  "dependencies": {
    "can-symlink": "^1.0.0",
    "fs-tree-diff": "^0.5.4",
    "heimdalljs": "^0.2.1",
    "heimdalljs-logger": "^0.1.7",
    "rimraf": "^2.4.3",
    "symlink-or-copy": "^1.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.40",
    "@types/node": "^7.0.10",
    "chai": "^3.4.0",
    "chai-files": "^1.4.0",
    "fixturify": "^0.3.1",
    "fs-extra": "^1.0.0",
    "mocha": "^2.3.3",
    "rollup": "^0.41.6",
    "rollup-plugin-commonjs": "^8.0.2",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-plugin-typescript": "^0.8.1",
    "tslint": "^4.5.1",
    "typescript": "^2.2.1"
  },
  "scripts": {
		"prepublish": "yarn run build && yarn run build:test",
    "build": "rollup --no-strict -c node.config.js",
    "build:test": "rollup --no-strict -c test.config.js",
    "test": "yarn run tslint && cp -R ./tests/fixtures ./dist/tests/ && mocha dist/tests/merge-trees-tests.cjs",
    "test:debug": "mocha debug dist/tests/merge-trees-tests.cjs",
    "build_and_test": "yarn run build:test && yarn run test",
    "tslint": "tslint --type-check -c tslint.json --project tsconfig.json"
  }
}
